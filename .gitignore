test.dat
compressed.out
perf.data*
# Anchored to the repo root and globbed, so build-release and friends are covered too, and
# docs/site/build.sh is not.
/build*
/debug*
cmake-build-*
.idea
install
.DS_Store
.vscode
.cache
.claude
tmp
docs/site/html
sz3_install
sz3_build
/test

# Python bytecode
__pycache__/
*.pyc
