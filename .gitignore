test.dat
compressed.out
perf.data*
# Anchor build/debug to repo root so they don't match docs/site/build.sh
# or docs/site/scripts/build_*.py
/build*
/debug*
cmake-build-*
# Anchored to the repo root, and glob so build-release and friends are covered too
.idea
install
.DS_Store
.vscode
tmp
.cache
docs/site/html
.claude
sz3_install
sz3_build
/test

# Python bytecode
__pycache__/
*.pyc
